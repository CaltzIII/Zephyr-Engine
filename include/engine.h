// engine.h

#include <board.h>
#ifndef ENGINE_H
#define ENGINE_H

// Этот файл может включать в себя общие определения и настройки для всего проекта.

// --- Общие включения ---
// Включаем базовые типы данных, если они понадобятся глобально.
// Часто лучше включать специфические заголовки в нужных .c файлах,
// но если какой-то базовый тип используется повсеместно, можно добавить здесь.
// #include <stdint.h>
// #include <stdbool.h>

// --- Общие константы ---

// Версия движка
#define ENGINE_NAME "Zephyr-Engine"
#define ENGINE_VERSION "0.1"
#define ENGINE_AUTHOR "Max Solokhin"

// Размер хэш-таблицы по умолчанию в мегабайтах (если будет реализована)
// #define DEFAULT_TT_SIZE 64

// Константы для оценки (если нужно централизованное управление)
// Лучше держать их в eval.c, но можно определить здесь, если они используются глобально.
// #define PAWN_VALUE 100
// #define KNIGHT_VALUE 320
// ...

// Максимальная глубина поиска по умолчанию
#define DEFAULT_MAX_DEPTH 64

// --- Глобальные переменные (объявления) ---
// Если в проекте используются глобальные переменные (например, основная доска),
// их объявления (не определения!) помещаются сюда с ключевым словом extern.
// Определения (с выделением памяти) должны быть в одном из .c файлов (например, в main.c).

extern Board g_board; // Объявление глобальной доски из main.c

// --- Прототипы глобальных функций ---
// Функции, которые должны быть доступны из разных частей проекта.

// Инициализация всего движка (из main.c)
void engine_init();

// --- Макросы для отладки ---
// Можно определить макросы, упрощающие отладку.

// #define DEBUG_PRINT(fmt, ...) fprintf(stderr, "[DEBUG] %s:%d: " fmt "\n", __FILE__, __LINE__, ##__VA_ARGS__)
// Использование: DEBUG_PRINT("Значение переменной x: %d", x);

// Безусловная отладочная печать
// #define INFO_PRINT(fmt, ...) printf("info string " fmt "\n", ##__VA_ARGS__); fflush(stdout);
// Использование: INFO_PRINT("Текущая глубина: %d", depth);


#endif // ENGINE_H